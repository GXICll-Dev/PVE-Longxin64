# PVE-Longxin64 项目总纲

> 本文件是本仓库的项目级开发与产品指导，作用类似 CLAUDE.md。
> 除非用户在当前任务中明确覆盖，所有开发者和 AI Agent 都应遵守本文件。
>
> 当前仓库仍处于绿地规划阶段。本文中的技术栈和目录结构是项目决策，不代表已经完成的实现。

## 1. 项目定位

PVE-Longxin64 是一个面向学校、培训机构和计算机教室的第三方 Proxmox VE 云教室管理平台。

平台不以复制完整 PVE 管理后台为目标，而是把 PVE 的虚拟化能力组织成教师和学校运维能够直接使用的云教室工作流：

- 管理员维护黄金教学模板及其不可变版本。
- 管理员按教室或课程批量创建学生虚拟桌面。
- 教师能够执行课前检查、整班开机、关机、快照、还原和下课收机。
- 瘦客户机作为学生终端，自动连接当前座位被分配的虚拟桌面。
- 所有批量操作都可追踪、可审计、可重试，并能清楚呈现部分失败。
- 教学镜像能够进行维护、验证、灰度发布、回滚和分批更新。

产品应优先解决“如何稳定地上完一节课”，而不是优先暴露 PVE 的底层概念。

### 1.1 核心价值

1. 一间教室可以在课前快速恢复到已知、可教学的状态。
2. 教师无需理解 PVE 节点、存储和 VMID，也能完成日常课堂操作。
3. 学生从瘦客户机登录后，只能连接分配给自己的桌面。
4. 管理员能维护少量模板，并安全地向大量学生桌面发布更新。
5. 任何批量或破坏性操作都不会成为不可见、不可恢复的黑盒。

### 1.2 初始假设

在获得真实部署环境信息前，按以下假设推进，并通过 ADR 记录后续变更：

- 首先面向学校局域网内的单组织部署。
- MVP 支持一个 PVE 集群，但领域模型保留 organization_id 和 cluster_id。
- 支持 PVE 8/9，实际兼容范围必须通过能力检测和测试矩阵确认。
- PVE 主机架构不因仓库名而预设为龙芯。
- 仓库名中的 Longxin64 暂按“龙芯 LoongArch64 瘦客户机兼容目标”处理。
- 瘦客户机优先采用 LoongArch64 Linux，运行全屏 Launcher 或浏览器 kiosk。
- 教学 VM 应安装 QEMU Guest Agent，并配置可用的远程桌面服务。
- 首先支持局域网场景；公网访问必须经过 VPN、零信任网关或等价安全边界。

### 1.3 非目标

MVP 不包含：

- 替代 PVE 原生后台的所有节点、Ceph、网络和 HA 管理能力。
- 自动跨多个 PVE 集群进行复杂调度。
- SaaS 计费、多租户计费或资源交易。
- GPU/vGPU、高帧率 3D 和专业音视频桌面的完整方案。
- 完整课堂屏幕直播、录制和监控。
- Proxmox Backup Server 灾备编排。
- 瘦客户机高级 OTA；只预留协议和数据模型。
- 通过任意 SSH 命令直接管理 PVE。

## 2. 产品组织方式

界面和领域模型必须以“云教室”而不是“PVE 虚拟机列表”为中心。

~~~text
组织/校区
└── 云教室
    └── 座位
        ├── 瘦客户机
        ├── 学生虚拟桌面
        ├── 当前模板版本
        └── 当前课程、连接和任务
~~~

以下对象必须分开建模：

- **座位 Seat**：教室中的物理或逻辑位置。
- **瘦客户机 ThinClient**：可替换的物理终端设备。
- **虚拟桌面 VirtualDesktop**：可重建、可迁移、可重新分配的 PVE VM。
- **学生或用户 User**：在某个课程场次中使用座位和桌面的人。

不得把 MAC 地址、座位号或 PVE VMID 当作平台全局主键。

## 3. 角色与权限

| 角色 | 主要能力 |
| --- | --- |
| 平台管理员 | 集群接入、凭据、系统策略、用户角色、全部审计 |
| 学校运维 | 教室、模板、桌面池、终端、批量任务和故障处理 |
| 教师 | 被授权教室的课前检查、开课、收机、单机重试和有限远程协助 |
| 学生 | 只查看并连接当前分配给自己的桌面 |
| 审计员 | 只读查看配置、操作记录和安全审计 |

权限规则：

- 权限必须在服务端校验；前端隐藏按钮不能替代授权。
- 权限范围至少支持组织、校区、教室和资源类型。
- 学生不能枚举其他学生、其他桌面或 PVE 基础设施。
- 教师不能默认发布模板、删除桌面池或管理 PVE 凭据。
- 删除、回滚、模板发布、强制停止和远程控制必须使用独立权限。

## 4. 推荐系统架构

MVP 采用“模块化单体 API + 独立任务 Worker + 桌面连接代理”，不要过早拆成大量微服务。

~~~text
管理员/教师浏览器
        │ HTTPS
        ▼
Web 控制台 ───────► Control Plane API ───────► PostgreSQL
                          │                         │
                          ├────► Task Worker ◄─────┘
                          │           │
                          │           ▼
                          │       PVE Adapter ─────► Proxmox VE API
                          │
瘦客户机 Launcher ───────┼────► Desktop Broker
                          │           │
                          ▼           ▼
                    Remote Gateway ──► 学生虚拟桌面
~~~

### 4.1 组件职责

- **Web 控制台**：管理员、运维和教师使用的管理界面。
- **Control Plane API**：认证、授权、领域规则、期望状态、任务创建和审计。
- **PVE Adapter**：集中封装 PVE API、版本差异、能力检测、错误归一化和 UPID 查询。
- **Task Worker**：执行克隆、开关机、快照、还原、模板发布、更新和对账。
- **Desktop Broker**：根据课程、座位、终端、用户和策略选择目标桌面。
- **Remote Gateway**：代理 RDP/VNC 等会话，不向终端暴露 PVE 管理网络。
- **Thin Client Launcher/Agent**：设备注册、心跳、桌面发现、连接拉起和断线重连。
- **PostgreSQL**：保存业务关系、期望状态、任务、幂等键、锁信息和审计。
- **可观测性模块**：结构化日志、指标、任务链路、PVE 请求耗时和告警。

### 4.2 建议技术栈

这是绿地项目的默认技术决策；更换时必须记录 ADR。

- 管理端：React + TypeScript + Vite。
- 路由、服务端状态和表格：TanStack Router、TanStack Query、TanStack Table。
- 后端与 Worker：Go，优先保证 linux/amd64 与 linux/loong64 可构建。
- API：REST + OpenAPI；任务进度使用 SSE，保留轮询降级。
- 数据库：PostgreSQL。
- 任务队列：优先使用 PostgreSQL 持久化任务和 Worker 租约；只有明确需要时再引入 Redis。
- 数据访问：显式 SQL、迁移和类型安全查询；避免隐藏关键事务行为的重 ORM。
- 部署：容器化控制平面；瘦客户机 Agent 提供独立架构构建产物。

### 4.3 建议目录

~~~text
/
├── AGENTS.md
├── README.md
├── web/                 # React 管理端
├── server/              # Go API、领域层和 PVE Adapter
├── worker/              # Go 后台任务；可与 server 共享内部包
├── agent/               # 瘦客户机 Launcher/Agent
├── api/                 # OpenAPI、生成代码和协议说明
├── deploy/              # Compose、反向代理和部署清单
├── docs/
│   ├── adr/             # 架构决策记录
│   ├── architecture/
│   └── operations/
└── tests/
    ├── integration/
    └── e2e/
~~~

共享 Go 代码可在确认模块边界后放入 internal；不要为了符合目录图而创建空目录。

## 5. 状态来源与对账

平台数据库和 PVE 承担不同职责：

- PVE 是节点、存储、虚拟机电源和任务执行结果的事实来源。
- 平台数据库是教室、座位、课程、模板版本、期望状态、权限和审计的事实来源。

虚拟桌面不能只保存一个 status 字段，至少要区分：

- desired_state：平台希望达到的状态。
- observed_state：最近从 PVE 观察到的状态。
- operation_state：当前平台任务的状态。
- last_reconciled_at：最后对账时间。
- config_hash：期望配置与实际配置的差异。

必须运行周期性 Reconciler：

1. 拉取受管 PVE 资源的实际状态。
2. 对比平台期望状态和资源标记。
3. 更新观察状态和漂移告警。
4. 只在规则明确且安全时自动修复。
5. 未知或孤儿资源只告警，不得自动删除。

## 6. 核心领域模型

- Organization：组织边界；MVP 为单组织。
- User、Role、Permission：身份和权限。
- PveCluster：接入点、版本、证书、凭据引用、能力和健康状态。
- PveNode、Storage、Network：从 PVE 同步的基础设施镜像。
- Classroom：教室、时区、默认网络和策略。
- Seat：教室座位编号与布局。
- ThinClient：设备 UUID、证书、架构、Agent 版本、IP、心跳和在线状态。
- Course、ClassGroup：课程和教学班。
- ClassSession：一次具体上课，包含起止时间、教室、模板版本和结束策略。
- DesktopPool：共享模板、规格、网络和还原策略的一组桌面。
- VirtualDesktop：平台 UUID 与 cluster_id + PVE VMID 的映射。
- DesktopAssignment：在指定时间内连接座位、终端、学生和桌面。
- Template、TemplateVersion：逻辑模板和不可变版本。
- BaselineSnapshot：平台认可、可回滚的基线。
- UpdateCampaign：模板升级目标、批次、暂停点和结果。
- RemoteSession：短期连接授权、协议、网关和撤销状态。
- Operation、OperationItem：批量父任务和单资源子任务。
- AuditEvent：不可变的安全与操作审计。

内部主键使用 UUID。PVE VMID 只在一个 cluster_id 内唯一。

## 7. 核心工作流

### 7.1 接入 PVE 集群

1. 管理员输入 API 地址、证书信任方式和最小权限 Token。
2. 后端校验证书、版本、身份和权限。
3. 检测节点、存储、网络、模板、快照和克隆能力。
4. 只允许选择专用 PVE Pool、存储和网络作为平台管理范围。
5. 保存加密凭据引用，不返回 Token 明文。
6. 创建首次同步任务并展示结果。

浏览器和瘦客户机永远不能直接持有 PVE Token。

### 7.2 模板生命周期

已发布模板不可变，禁止静默覆盖。

~~~text
DRAFT
  → BUILDING
  → VALIDATING
  → PUBLISHED
  → DEPRECATED
  → RETIRED
~~~

构建或验证失败分别进入 BUILD_FAILED 或 VALIDATION_FAILED。

标准更新流程：

1. 从当前版本克隆隔离的维护 VM。
2. 安装系统补丁、课程软件和资源。
3. 执行系统清理；Windows 使用 Sysprep，Linux 清理 machine-id/Cloud-init 状态。
4. 关机并生成新的 PVE 模板 VMID。
5. 创建少量金丝雀桌面。
6. 验证启动、QEMU Guest Agent、网络、远程桌面和关键应用。
7. 发布新的不可变 TemplateVersion。
8. 新课程默认使用新版本；既有课程不被隐式切换。
9. 通过 UpdateCampaign 分波次更新，失败时停止扩散。

### 7.3 创建桌面池

创建前必须预检：

- 模板版本有效且已发布。
- 目标节点、存储和网络可用。
- 预计磁盘、内存和 VMID 数量充足。
- 存储是否支持链接克隆和快照。
- 当前是否存在冲突任务。
- 并发限制是否允许启动本批次。

支持：

- 链接克隆：创建快，但依赖模板和存储能力。
- 完整克隆：占用更多空间，但隔离和迁移能力更强。

克隆策略必须由桌面池显式保存，不能根据环境静默切换。

### 7.4 课程场次

课程状态：

~~~text
DRAFT → PREPARING → READY → OPENING → ACTIVE → CLOSING → CLOSED → ARCHIVED
~~~

课前准备：

1. 检查终端在线、桌面存在、模板版本、资源容量和冲突任务。
2. 根据策略执行终端唤醒、桌面还原和分波次预启动。
3. 检查 VM 状态、Guest Agent、网络和远程桌面端口。
4. 对异常座位给出原因，并允许排除后继续。

开课后：

- Broker 为每个座位或学生选择桌面。
- 签发短期、一次性、目标受限的连接授权。
- 教师能看到座位级状态和失败原因。

下课时：

- 先撤销或结束远程会话。
- 默认请求优雅关机。
- 根据桌面池策略保留、回滚或重新创建桌面。
- 取消任务不代表撤销已经完成的单机操作。

### 7.5 桌面持久化与还原

每个 DesktopPool 必须选择一种策略：

- **RECREATE**：下课后删除并从指定模板版本重建。推荐作为无状态教室默认方案。
- **ROLLBACK_BASELINE**：回滚到基线快照；必须确认存储支持且空间充足。
- **PERSISTENT**：保留桌面，不自动还原，适合长期个人桌面。

用户资料不能只存在于会被还原的系统盘。需要保留时使用独立数据盘、漫游配置或网络家目录。

必须区分：

- 快照回滚。
- 从模板重新创建。
- 从 PBS 备份恢复。

界面和 API 不得将三者模糊成同一个“还原”动作。

### 7.6 瘦客户机

- 首次启动通过一次性注册码注册。
- 注册后使用设备证书或等价设备身份，不长期依赖 MAC 地址。
- 上报设备 UUID、架构、Agent 版本、IP、硬件摘要、当前会话和心跳。
- 支持固定座位绑定和设备更换后的重新绑定。
- 本地不保存 PVE Token、模板权限或长期管理员密码。
- Launcher 支持全屏 kiosk、断线重连和课程结束后返回登录页。
- 学生网络、桌面网络、网关网络和 PVE 管理网络必须隔离。

## 8. 远程桌面策略

采用可插拔 Desktop Broker 和 Gateway，不把 PVE noVNC 当作日常学生 VDI。

推荐顺序：

1. **原生 RDP / FreeRDP**：受控瘦客户机的默认方向，适合局域网和低资源终端。
2. **Apache Guacamole + RDP**：浏览器零安装访问和兼容入口。
3. **PVE noVNC**：只用于安装、故障恢复和管理员应急控制台。
4. **SPICE**：作为 Linux、USB 重定向或特定场景的可选协议。

连接流程：

1. 终端以设备证书或用户身份登录平台。
2. Broker 根据课程、座位和当前分配确定目标桌面。
3. VM 未运行时创建启动任务。
4. 同时验证 PVE 状态、Guest Agent、网络和目标协议端口。
5. Broker 签发短期、一次性、只允许连接目标桌面的授权。
6. Gateway 建立会话，终端不接触 PVE 管理网和长期来宾凭据。
7. 下课、解绑或管理员撤销后，立即禁止新连接并终止已有授权。

网关策略必须可控制剪贴板、磁盘、打印机、USB 和音频重定向。

教师观察或控制学生屏幕必须单独授权、明确告知并记录审计。

## 9. 任务系统

所有批量或耗时操作必须异步执行。HTTP 请求只负责校验和创建任务。

API 接收任务后返回 202 和 operation_id，不能把“已受理”描述成“已成功”。

### 9.1 状态机

~~~text
QUEUED
  → VALIDATING
  → RUNNING
  → WAITING_PVE
  → VERIFYING
  → SUCCEEDED
~~~

异常终态：

- PARTIALLY_SUCCEEDED
- FAILED
- CANCEL_REQUESTED
- CANCELLED

每个批量 Operation 必须拆成多个 OperationItem。界面分别显示成功、失败、跳过、执行中和未知。

### 9.2 幂等与恢复

- 每个写 API 接受 Idempotency-Key。
- 重复请求返回同一任务，不得重复创建 VM 或快照。
- PVE 返回 UPID 后立即持久化。
- Worker 重启后根据 UPID 恢复跟踪，不能盲目重发命令。
- 重试前先查询实际状态；目标已达到时按成功收敛。
- 重试使用有限次数、指数退避和随机抖动。
- 认证、权限和参数错误不得无限重试。
- 取消采用协作式取消；无法安全取消的 PVE 任务继续跟踪到终态。

### 9.3 并发控制

- 同一 VM 同时最多执行一个互斥操作。
- 集群、节点、存储和桌面池分别设置并发上限。
- 批量启动和克隆必须分波次，避免开机风暴和克隆风暴。
- 教室级锁和 VM 级锁使用固定顺序，避免死锁。
- 数据库实体使用 resource_version 做乐观并发控制。
- VMID 使用平台预留和唯一约束，处理 PVE nextid 并发冲突。
- PVE 不可用时启动熔断，停止继续制造重复操作。

## 10. PVE 集成边界

- 所有 PVE 调用只能经过 PVE Adapter。
- HTTP Handler 和 UI 组件不得直接拼接 PVE API 请求。
- 优先使用正式 REST API；SSH 只能作为有明确设计和审计的例外适配器。
- 必须校验 PVE TLS 证书，支持导入学校内部 CA。
- 禁止默认关闭 TLS 校验。
- 每个集群使用独立服务账户和 API Token，并启用 privilege separation。
- Token 使用应用主密钥或密钥服务加密保存。
- PVE 请求记录关联 ID、节点、目标 VMID、UPID、耗时和脱敏结果。
- 平台只操作显式纳管的 PVE Pool、Tag、存储和网络。
- 受管 VM 同时使用平台 UUID、PVE Pool 和 Tag 标识，不能只靠名称识别。
- 已存在 VM 必须通过显式“纳管”流程导入。
- 快照、链接克隆和存储特性通过能力检测确认，不能只依据版本号猜测。

PVE 账户不得为方便直接授予 Administrator。权限应按实际功能限制到专用 Pool、存储和网络范围，并在安装时验证目标 PVE 版本的权限名称。

## 11. 安全与审计

- 所有流量使用 TLS。
- PVE Token、设备私钥、远程桌面长期密码不得进入浏览器、日志或错误响应。
- 敏感配置只保存加密值或密钥引用。
- Web 会话具备安全 Cookie、CSRF 防护、过期和撤销机制。
- 设备注册使用短期一次性码，注册后轮换为设备证书。
- 所有授权判断在服务端完成，并记录授权失败。
- 审计记录至少包含操作者、来源 IP、动作、目标范围、参数摘要、原因、关联任务和结果。
- 审计记录不能由普通管理员修改。
- QEMU Guest Agent 只开放明确需要的能力，禁止无边界任意命令执行。
- Windows、RDS/VDI、Office 和教学软件许可必须在上线前确认。
- 课堂观察、录制、剪贴板和文件重定向必须符合学校隐私制度。

高风险操作包括：

- 删除桌面、模板、快照或桌面池。
- 快照回滚和从模板重建。
- 大范围强制停止。
- 模板发布或大规模版本切换。
- 教师远程控制学生桌面。

这些操作必须：

1. 在服务端重新校验权限和目标范围。
2. 执行资源、容量、课程和冲突预检。
3. 展示受影响对象数量和数据损失。
4. 要求明确二次确认和操作原因。
5. 使用幂等键和对象锁。
6. 创建完整审计事件。

## 12. 前端信息架构

全局导航：

~~~text
总览
云教室
模板与镜像
终端设备
任务中心
基础设施
自动化与策略
审计与设置
~~~

- **总览**：教室就绪率、上课状态、异常座位、离线终端、版本偏差和容量告警。
- **云教室**：课堂控制台、座位、终端绑定、桌面实例、策略和历史。
- **模板与镜像**：黄金模板、不可变版本、维护副本、灰度发布和回滚。
- **终端设备**：心跳、IP、架构、Agent 版本、座位和远程诊断。
- **任务中心**：父任务、单机结果、失败重试、日志和导出。
- **基础设施**：PVE 集群、节点、存储、网络和资源池，仅面向管理员。
- **自动化与策略**：课前预热、课后还原、并发、快照保留和更新窗口。
- **审计与设置**：用户、角色、凭据、操作审计和系统配置。

### 12.1 云教室详情

默认进入课堂控制台，而不是 VM 配置页。

必须分别展示：

1. 瘦客户机连接状态。
2. 虚拟桌面电源状态。
3. 模板版本合规状态。
4. 当前任务或维护状态。

主要区域：

- 顶部就绪摘要。
- 课前检查、整班开机、整班还原、下课收机等高频操作。
- 座位表格；座位图只作为辅助视图。
- 选中对象后的固定批量操作栏。
- 不离开筛选上下文的单座位详情侧栏。

### 12.2 危险操作 UX

- 按钮必须写准确动词和数量，例如“还原 32 台学生桌面”。
- “当前页”和“全部筛选结果”必须是两个明确范围。
- 强制停止放在优雅关机之后的次级入口。
- 大范围数据变更要求输入教室名或目标数量，并填写原因。
- 危险按钮不获得默认焦点。
- 禁用操作必须解释原因。
- 不能只用红色表示危险。

### 12.3 批量任务反馈

- Toast 只提示任务已创建、完成或需要关注。
- 详细结果放在跨页面常驻的任务抽屉和任务中心。
- 展示父任务进度、当前阶段和每台机器的结果。
- 支持只重试失败项、复制任务 ID 和导出结果。
- 刷新或断线重连后仍能恢复任务状态。
- 没有真实进度时显示阶段和完成数量，不伪造百分比或预计时间。
- 实时更新优先使用 SSE，并提供轮询降级。

## 13. 前端库选择

依据 pick-ui-library skill，采用以下默认映射：

| 任务 | 默认库 | 边界 |
| --- | --- | --- |
| Dialog、Popover、Menu、Select | Base UI | 负责无障碍、焦点和关闭行为 |
| 命令面板 | cmdk | 快速定位教室、机器和任务 |
| Toast | Sonner | 不承载批量任务详情 |
| 大型列表 | Virtuoso | 与服务端筛选、排序和分页配合 |
| 客户端全局状态 | zustand | 只存 UI 状态和用户偏好 |
| 条件样式 | clsx | 简单条件组合 |
| 组件变体 | cva | 按钮、标签等类型安全变体 |
| 实时曲线 | Liveline | 只用于流式指标 |
| 历史图表 | Recharts | 非实时趋势和统计 |
| 座位布局编辑 | dnd kit | 不替代主要表格操作 |
| 深浅主题 | next-themes | 技术栈兼容时使用 |

精选列表未覆盖但项目需要：

- TanStack Query：服务端状态、缓存和 Mutation。
- TanStack Table：复杂表格模型。
- noVNC：管理员应急控制台，由后端签发短期票据。

简单交互优先使用 CSS。首版不把 Motion 作为核心依赖；只有真实手势、可中断弹簧或复杂布局动画时才引入。

## 14. UI 与动效规则

依据 emil-design-eng skill，本项目是高频运维工具，动效必须克制：

- 搜索、表格选择、导航、快捷键和实时状态刷新不使用装饰性动画。
- 键盘触发的高频操作不播放开关动画。
- 按钮按下反馈为 100–160ms。
- Tooltip 和小 Popover 为 125–180ms。
- Dialog 通常为 180–220ms。
- 常规 UI 动效不得超过 300ms。
- 不使用 ease-in；进入使用强 ease-out，屏幕内移动使用 ease-in-out。
- 只动画 transform 和 opacity，避免动画 width、height、margin 或大型表格布局。
- Popover 从触发点出现；居中 Dialog 保持中心原点。
- 不为实时 CPU、内存数字或大型列表添加装饰性入场动画。
- 默认使用可中断的 CSS transition，不对动态 UI 滥用 keyframes。
- 完整支持 prefers-reduced-motion。
- Hover 动效必须限制到支持 hover 且具有精细指针的设备。

可访问性目标为 WCAG 2.2 AA：

- 所有功能均可使用键盘完成。
- 状态同时使用文字、图标和颜色表达。
- 焦点始终可见，Dialog 关闭后焦点返回触发元素。
- 图标按钮必须提供可读名称。
- 批量任务只播报汇总变化，避免逐台刷屏。
- 重要时间显示明确时区和绝对时间。

## 15. 性能规则

- 机器、任务和日志列表必须虚拟化。
- 筛选、排序、搜索和分页在服务端完成。
- 实时状态以对象级补丁更新，并把高频变化合并为约 250–500ms 一批。
- 不因后台数百台机器变化而重绘整个表格。
- 表格内不为每台机器持续绘制实时图表。
- noVNC、图表、长日志和高级基础设施页面按需加载。
- 搜索请求可取消并防抖；筛选条件保存在 URL。
- 只有可逆的标签和备注可使用乐观更新。
- 开关机、还原、删除和模板发布必须等待服务端任务确认。
- 数据断线或陈旧时明确显示最后更新时间。

## 16. API 约定

- API 统一放在 /api/v1。
- 使用 OpenAPI 作为前后端契约来源。
- 所有 ID 使用字符串 UUID；外部 VMID 单独表达。
- 时间存储为 UTC，API 使用 RFC 3339，前端按教室时区显示。
- 批量操作返回 202、operation_id 和可查询地址。
- 所有写接口支持 Idempotency-Key。
- 列表统一支持游标或稳定分页、服务端过滤和排序。
- 错误返回稳定 error_code、用户可读信息、request_id 和可选字段级详情。
- 不把 PVE 原始错误、内部堆栈或敏感字段直接返回前端。
- SSE 事件必须可重连；事件包含 operation_id、item_id、sequence 和时间戳。
- 删除、还原、发布等接口必须支持预检或 dry-run。

## 17. 可观测性

必须具备：

- JSON 结构化日志和贯穿 API、Worker、PVE UPID 的 correlation_id。
- API、Worker、PVE 和 Gateway 的延迟与错误指标。
- 任务队列长度、运行时长、重试、失败和卡住任务指标。
- PVE 集群、节点、存储容量、证书到期和同步延迟告警。
- ThinClient 在线率、心跳延迟、Agent 版本和连接失败指标。
- 审计日志与普通应用日志分离保存。

日志不得包含 Token、Cookie、设备私钥、完整桌面密码或学生敏感数据。

## 18. 测试策略

### 18.1 单元测试

- 领域状态机和权限规则。
- 模板版本不可变约束。
- 幂等键、批量拆分、锁和重试决策。
- PVE 错误归一化和能力检测。
- Broker 分配和连接授权。

### 18.2 集成测试

- 使用可编程 Fake PVE Server 覆盖成功、超时、权限错误和 UPID 恢复。
- PostgreSQL 迁移、事务、任务租约和 Worker 崩溃恢复。
- API Token、TLS 内部 CA 和凭据加密。
- SSE 断线重连和事件顺序。

### 18.3 端到端测试

- 集群接入到资源同步。
- 模板导入、桌面池创建和批量克隆。
- 课前检查、整班开机、学生连接和下课收机。
- 快照、回滚、重建和只重试失败项。
- 模板金丝雀、发布、灰度更新和失败停止。
- 越权、重复提交、页面刷新和 Worker 重启。

### 18.4 实验室验证

高风险 PVE 操作必须在隔离测试集群验证：

- 节点离线。
- PVE API 短时断线。
- 存储空间不足。
- 克隆或快照部分失败。
- Worker 在收到 UPID 后崩溃。
- 终端离线或连接票据过期。
- 50 台桌面分波次启动。
- LoongArch64 瘦客户机的 FreeRDP、浏览器和 Agent 兼容性。

## 19. 实施路线图

### Phase 0：决策与工程基础

- 确认 PVE 版本、集群规模、存储、网络和瘦客户机硬件。
- 用 ADR 固化后端、数据库、远程协议和认证选择。
- 建立 Go、React、OpenAPI、迁移、测试和 CI 骨架。
- 建立 Fake PVE Server 和安全配置加载。

退出标准：本地可启动 Web、API、Worker 和 PostgreSQL，CI 能执行测试与静态检查。

### Phase 1：PVE 接入与只读资源

- 集群凭据和 TLS 信任。
- PVE Adapter、能力检测和资源同步。
- 节点、存储、网络、模板和 VM 只读页面。
- 基础认证、管理员角色和审计。

退出标准：平台能安全接入测试 PVE，准确展示资源与同步健康。

### Phase 2：单桌面生命周期与任务引擎

- Operation、OperationItem、幂等键、锁、Worker 和 SSE。
- 单 VM 开机、优雅关机、强制停止、重启、克隆、快照和回滚。
- UPID 持久化、重启恢复、错误归一化和单项重试。

退出标准：Worker 中断后可继续跟踪任务，重复请求不重复执行。

### Phase 3：云教室与批量工作流

- Classroom、Seat、DesktopPool、VirtualDesktop。
- 批量克隆、分波次开机、课前检查和下课收机。
- 教师课堂控制台、任务抽屉和部分失败处理。
- RECREATE、ROLLBACK_BASELINE、PERSISTENT 三种策略。

退出标准：能从模板创建参考规模教室，并完成完整上课与收机流程。

### Phase 4：瘦客户机与远程桌面

- 设备一次性注册、证书、心跳和座位绑定。
- Desktop Broker 和短期连接票据。
- FreeRDP 或 Guacamole 路径。
- noVNC 管理员应急控制台。
- Launcher 全屏、断线重连和会话撤销。

退出标准：学生终端无法接触 PVE Token，并能稳定连接被分配桌面。

### Phase 5：模板版本与更新

- TemplateVersion 状态机、维护 VM、验证和发布。
- 金丝雀桌面、UpdateCampaign、分批更新和失败暂停。
- 基线快照、保留策略和漂移检测。

退出标准：新模板发布不改变既有课程，失败金丝雀不会扩散。

### Phase 6：强化与交付

- OIDC/LDAP/AD、MFA 和更细 RBAC。
- 完整监控、告警、备份和运维手册。
- 安装向导、升级方案和兼容矩阵。
- 压力、故障注入、安全和可访问性测试。
- LoongArch64 终端构建与真实硬件验收。

退出标准：满足上线安全、稳定性、可恢复性和运维要求。

## 20. MVP 验收标准

- 从一个模板批量创建至少 50 台学生桌面，每台都有明确终态。
- 教师提交批量操作后 1 秒级获得任务 ID；耗时操作不阻塞 HTTP。
- 重复提交同一个请求不会创建重复 VM、快照或任务。
- Worker 在克隆、启动或回滚期间重启后，能根据已保存 UPID 恢复跟踪。
- 50 台桌面分波次启动时，控制平面仍可响应，并发数可配置。
- PVE 短时断线后能恢复对账，不盲目重放，不把未知状态报告为成功。
- 学生只能连接分配给自己的桌面。
- 终端和浏览器中不存在 PVE Token。
- 课程关闭或授权撤销后，远程桌面连接授权立即失效。
- 新模板版本不会修改既有课程绑定，失败金丝雀不能正式发布。
- RECREATE 或快照回滚不会删除标记为持久化的数据盘。
- 所有删除、回滚、强制停止、模板发布和远程控制都有审计事件。
- 存储或内存不足时，在大规模操作前明确拒绝或降低批次。
- 节点故障、部分克隆失败或终端离线不会阻塞其他可用座位。
- 教师无需进入 PVE 基础设施页面即可完成课前检查、开课和收机。
- 刷新页面后仍可追踪批量任务，并能只重试失败项。

## 21. 编码与协作约束

- 文档和用户界面默认使用简体中文；代码标识符、API 字段和提交信息使用清晰英文。
- 不得在 React 组件、HTTP Handler 或定时器中散落 PVE API 调用。
- 领域规则放在服务层或领域层，PVE 细节只存在于 Adapter。
- 所有数据库变更使用迁移；迁移需要向前兼容和回滚说明。
- 所有外部调用设置超时、取消、有限重试和关联 ID。
- 不以字符串匹配 PVE 错误作为唯一业务判断，先在 Adapter 中归一化。
- 不直接删除未知 PVE 资源，不自动纳管未标记 VM。
- 不把快照称为备份。
- 不把“请求已受理”写成“操作成功”。
- 新增破坏性功能时，权限、预检、幂等、锁、审计和失败恢复必须一起设计。
- 新增页面时，加载、空状态、部分失败、无权限、断线和陈旧数据状态必须一起完成。
- 不为了动效牺牲高频操作效率或表格性能。
- 在没有真实环境时使用 Fake PVE，不在开发代码中硬编码生产凭据。

## 22. Skills 使用约定

仓库已安装 emilkowalski/skill 套件。按以下方式使用：

- **emil-design-eng**：设计或审查管理端 UI、组件、反馈和微交互时使用。
- **pick-ui-library**：只有在明确进行前端库选型时显式调用；先检查 package.json。
- **apple-design**：只在手势、拖拽、抽屉、弹簧或物理感交互确有价值时使用。
- **animation-vocabulary**：用于确认动画效果的准确名称，不用于实现。
- **find-animation-opportunities**：功能页面完成后进行只读机会分析。
- **improve-animations**：里程碑阶段进行全局动效审计和改进计划，不直接修改源码。
- **review-animations**：具体动效实现完成后进行严格代码审查。

不得为了“使用 skill”而给高频运维界面添加不必要动画。

## 23. Definition of Done

一个功能只有同时满足以下条件才算完成：

- 业务行为和权限边界明确。
- API 契约和错误语义已更新。
- 异步任务具有幂等、超时、锁、恢复和部分失败处理。
- 危险操作具有预检、确认、审计和影响范围。
- UI 包含加载、空、成功、失败、无权限和陈旧状态。
- 关键路径有单元测试；跨组件路径有集成或端到端测试。
- 日志、指标和审计足以定位问题。
- 不泄露 PVE、设备或学生凭据。
- 文档、部署或运维说明同步更新。
- 通过键盘、减少动态效果和基础可访问性检查。
- 已在 Fake PVE 或隔离实验集群验证，不以模拟结果冒充真实 PVE 成功。

## 24. 开工前必须确认的问题

以下信息会影响实现，但不阻塞项目骨架和 Fake PVE 开发：

1. 目标 PVE 版本、节点数量和是否使用集群。
2. 存储类型、容量、快照和链接克隆能力。
3. 学生 VM 的操作系统、软件、Guest Agent 和许可证。
4. 瘦客户机的具体龙芯型号、LoongArch 版本、内存、显卡和 Linux 发行版。
5. 默认采用 FreeRDP、Guacamole、SPICE 还是组合方案。
6. 是否需要 USB、音频、打印机、剪贴板和文件重定向。
7. 教室规模、同时在线人数和网络带宽。
8. 是否已有 AD、LDAP、OIDC 或校园统一认证。
9. 学生数据使用独立数据盘、网络家目录还是无状态模式。
10. 是否允许教师观察或控制学生屏幕，以及相应隐私制度。

在这些问题确认前，所有相关实现都应保持适配器边界，避免把环境假设写死。
